# [修复] 生产环境配置：API 路径已含 /api 前缀（见 src/api/*.js），baseURL 必须留空，
# 否则会拼接成 /api/api/v1/... 导致后端 404。Nginx 已按原样透传完整 URI。
VITE_APP_TITLE=秒杀系统
VITE_API_BASE=
# [修复] 与后端 config.prod.yaml 的 sign_secret 保持一致（登录/注册虽免签，登录后的业务接口需要）
VITE_SIGN_SECRET=miaosha-sign-secret-2026