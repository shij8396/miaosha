# [修复] Grafana Dashboard 自动加载配置
apiVersion: 1
providers:
  - name: 'miaosha'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards